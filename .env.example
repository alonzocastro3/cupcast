# Copy to .env — never commit .env itself

# PostgreSQL (used by docker-compose and backend)
POSTGRES_USER=cupcast
POSTGRES_PASSWORD=cupcast
POSTGRES_DB=cupcast

# Frontend public var (passed into Next.js at build/runtime)
NEXT_PUBLIC_API_URL=http://localhost:8000
